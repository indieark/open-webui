# `.ui/` — UI 风格入口

本目录按 `00000-model/02-项目模板` 的本地项目骨架保留。

当前仓库第一阶段不做 Open WebUI 前端 UI 定制；官方 UI 保持在 `upstream/open-webui/` 中。

如未来需要私库 UI 定制，应先确认能否通过 Open WebUI 配置、静态资源、theme 或根层 patch 承载，并在 `docs/compatibility-layer.md` 登记边界。IndieArk 共享 UI 规范参考：

```text
C:\Vibe_Coding\IndieArk\00000-model\01-复用资产\.ui\
```
